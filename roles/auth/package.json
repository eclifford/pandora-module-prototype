{
  "name": "@pandora/auth",
  "version": "1.0.0",
  "description": "Provides authentication capabilities, fullfils the AUTH role",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
